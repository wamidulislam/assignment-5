1.Answer: We can redeclare and change value of var but var is very old, let can redeclare and change value, const can't change and redeclare

2.Answer: Spread operator spreed all elements of an array and object. Using spread operator we can easily add or copy.

3.Answer: Map is a special loop which work on every element of an array and create a new array by the condition or operation we apply. for each is just like map but it does'nt return new array. Filter take from array based on condition and return them as a new array.

4.Answer: Arrow function is a simple function. On function we have to write function but in arrow function we use => instead of function. It keep our code clean.

5.Answer: Template literals allow us to write multi line code. we can write it using `` this.